{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - {{ parent() }} {% endblock %}

{% block content %}
    <div class="compte row">
        <h1>Modifier mon mot de passe</h1>
        <a href="{{path('account')}}">Retourner dans mon compte.</a>
        <hr>
        <div class="col-md-6">
            <div class="btn-info">{{sms_password}}</div>
            {{form(form)}}
        
        </div>
    </div>
{% endblock %}
